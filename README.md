# AuthApp

A modern authentication and authorization application built with **Spring Boot**, **Spring Security**, **JWT**, **MySQL**, **React**, and **TypeScript**.

The project demonstrates a production-style authentication workflow with secure token-based authentication, role-based access control, protected routes, and a responsive frontend.
